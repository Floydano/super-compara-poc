apiVersion: v1
kind: Service
metadata:
  name: super-compara-backend
  namespace: super-compara
  labels:
    app: super-compara-backend
spec:
  type: ClusterIP
  selector:
    app: super-compara-backend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
